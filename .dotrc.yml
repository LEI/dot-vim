dir: ".vim"
link: "[^.]*:.vim"
lines:
  - path: ".vim/vimrc"
    line: "source ~/.vim/init.vim"
    # if filereadable(expand('~/.vim/init.vim')) | source ~/.vim/init.vim | endif
  - path: ".vim/gvimrc"
    line: "source ~/.vim/ginit.vim"
    # if filereadable(expand('~/.vim/init.vim')) | source ~/.vim/init.vim | endif
